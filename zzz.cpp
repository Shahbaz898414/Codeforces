#include <iostream>
#include <vector>
#include <algorithm>

class Solution {
public:
    int rob(std::vector<int>& nums) {
        
    }
};

int main() {
    // Example usage
    Solution sol;
    std::vector<int> nums = {1, 2, 3,1}; // Example input
    int maxAmount = sol.rob(nums);
    std::cout << "Maximum amount that can be robbed: " << maxAmount << std::endl;

    return 0;
}
